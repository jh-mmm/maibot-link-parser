# 更新日志

## [1.4.0] - 2026-08-25

### 新增
- **新增 Pixiv 平台解析支持**（适配自 `Zhalslar/astrbot_plugin_parser` 并针对 MaiBot 和 OneBot 深度优化）：
  - **插画与多图画集**：支持提取单图/多图列表，配合合并转发发送多图画集。
  - **漫画解析**：支持漫画系列进度信息提取（系列名称、话数），支持配置 `max_manga_pages` 限制。
  - **动图 (Ugoira) 合成**：自动下载帧序列压缩包并使用 Pillow 合成为动态 GIF，在 QQ 聊天中可直接动态播放。
  - **小说解析**：提取小说标题、画师/作者、字数统计、简介与正文摘录，自动清理特殊分页与注音语法。
  - **R18 / NSFW 安全控制**：支持 `blur`（高斯模糊打码封面）、`ignore`（拦截并提示）、`send`（正常发送）。
  - **网络与代理支持**：支持 HTTP/HTTPS 代理（`proxy`）及图片反向代理（`img_proxy`，如 `i.pixiv.re`），图片下载自动携带防盗链 Header。
  - **触发形式丰富**：支持 `pixiv.net/artworks/...`、`member_illust.php`、`novel/...` 及纯文本 PID/PixivID 触发（如 `pid 123456`）。
- **配置体验与 WebUI 全面中文化**：
  - **WebUI 原生中文界面**：在 `plugin.py` 中引入 `json_schema_extra` 与 `ClassVar` 规范，所有配置项均已具备中文名称（`label`）、模式下拉菜单（`x-widget: select`）、多行文本输入（`input_type: textarea`）与密码脱敏（`input_type: password`），并在 WebUI 设置页面直接渲染详细的中文功能注释。
  - **`config.toml` 内嵌中文注释**：配置文件中内嵌全套详细中文注释、参数含义、取值范围与获取教程，无需翻阅文档即可在本地直接编辑修改。

## [1.3.0] - 2026-08-25

### 新增
- 增加分平台独立的访问控制机制（群/用户黑白名单），知乎、微博、YouTube、Twitter(X) 均可单独配置：

  - 支持 `off`（不限制）、`whitelist`（白名单）、`blacklist`（黑名单）三种模式。
  - 单个平台内群维度与用户维度为「与」关系，均通过才触发该平台解析。
  - 各平台规则完全解耦、独立生效；私聊消息自动跳过群维度校验；被拦截消息不阻断后续 AI 正常回复。
  - 自动去除号码首尾空格并兼容字符串与数字配置。

## [1.2.0] - 2026-07-25

### 修复
- 修复知乎解析中 403 错误。

### 新增
- 增加解析成功/失败提示，方便排查错误。

## [1.1.0] - 2026-07-22

### 修复
- 在 `_manifest.json` 中补充 `dependencies` 声明（aiohttp / beautifulsoup4 / curl_cffi / yt-dlp），便于 Host 在加载阶段校验缺失依赖；`requirements.txt` 同步补齐此前遗漏的 `aiohttp` 并与 manifest 版本对齐。

### 仓库整理
- 移除开发期的测试脚本 `test_parsers.py` 与 Python 字节码缓存（`__pycache__/`、`*.pyc`）.

## [1.0.9] - 2026-07-22

### 修复
- 为 `chat.receive.before_process` 钩子显式设置 `timeout_ms=120000`，避免解析+下载+发送的同步流程被钩子默认超时取消，导致「部分结果 + AI 双回复」。

## [1.0.8] - 2026-07-22

### 改进
- 知乎回答与专栏现提取正文内的**全部图片**（原仅取首图），配合合并转发可完整展示图集。
- 解析文本的互动统计改用统一的 `stats_text()` 格式化：补回此前被丢弃的 YouTube 播放量，并以「万/亿」中文计数显示。
- `max_content_length` 与 `timeout` 配置项此前在多个解析器中被写死，现已真正生效（知乎/微博/Twitter/YouTube）。
- 抽取公共图片下载方法，消除逐条发送与合并转发之间的重复逻辑。

## [1.0.7] - 2026-07-22

### 新增
- 知乎解析结果与多图结果现以合并转发方式发送，避免文本与多张图片逐条刷屏。
- 新增 `merge_send` 配置项（默认开启）；关闭或发送失败时自动回退为逐条发送。

## [1.0.6] - 2026-07-22

### 修复
- 修复了知乎部分文章因为没有登录 Cookie 导致报错 403 的问题。
- 修复了微博图片返回 403 的问题。